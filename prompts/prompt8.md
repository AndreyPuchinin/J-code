Перепиши ComandPath.get_line_and_pos() так, чтобы она корректно определяла позицию и строку команды (объекта CommandPath) в общем коде (BASE_CODE.json), ориентируясь на карту скобок (self._simple_parse_brackets()) и сам код. 

Должно работать так:

1.Если UnknownCommand, например:

[
    true
]

Должно выдать

Ошибка 1/12:
Ошибка синтаксиса!
-Файл: 'Games/BASE_CODE.json'
-Команда: UnknownCommand
-Строка: 1 #(сейчас 1)
-Позиция: 4 #!! (сейчас 0, видимо нет привязки к коду)
-Текст ошибки: Неизвестная команда 'True'.

2.Если ошибка в команде include, например:

[
    {"include": 0}
]

Должно выдать

Ошибка 1/12:
Ошибка синтаксиса!
-Файл: 'Games/BASE_CODE.json'
-Команда: include
-Строка: 1 #(сейчас 1)
-Позиция: 5 #с перепрыгиванием '{'!! (сейчас 0, видимо нет привязки к коду)
-Текст ошибки: Неизвестная команда 'True'.

3.Если ошибка в команде game_field, например:

[
    {"game_field": ""}
]

Должно выдать

Ошибка 1/12:
Ошибка синтаксиса!
-Файл: 'Games/BASE_CODE.json'
-Команда: include
-Строка: 1 #(сейчас 1)
-Позиция: 5 #с перепрыгиванием '{'!! (сейчас 0, видимо нет привязки к коду)
-Текст ошибки: Неизвестная команда 'True'.


!!!АЛГОРИТМ-ПЛАН ФУНКЦИИ!!!

То есть нужно
1.Определить текущую команду
2.Определить её координаты в коде, согласно cmd_path
3.Найти "анатомически то же место" в карте скобок bracket_map
4.Получить из последнего координаты команды в виде "строка, позиция в строке"

Ориентируйся на файл /workspaces/J-code/J-sub_code/errors.log: если в нем есть строки > чем количество строк в /workspaces/J-code/Games/BASE_CODE.json, ЗНАЧИТ ТЫ ОШИБСЯ.
Если в нем есть отрицательные строки или позиции, ЗНАЧИТ ТЫ ОШИБСЯ.
Если в нем есть строки или позиции "<не найдено>", ЗНАЧИТ ТЫ ОШИБСЯ!
Если в нем комбинация "строка-позиция" повторяется более одно раза, ЗНАЧИТ ТЫ ОШИБСЯ!