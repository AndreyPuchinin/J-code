Я хочу, чтобы при коде

[
    {"game_field": [[""]]}
]

Логиовалась не просто ошибка, а чтобы в ней непременно были координаты не только самого "game_field"'а, а еще и [""] и "". 

Но как это лучше организовать в текущем коде? Ведь можно подключить code -> CommandGameField, но это нарушает парадигму ООП о том, что классы должны знать ровно то, за что отвечают...

================

Да, давай сделаем класс Ошибки и перепишем применение везде на эту логику

================

Допиши логику "-Путь"в логах ошибок так, чтобы вместо "-Путь" выводилось "-Подстрока параметра" и "-Подпозиция параметра"

Например, для кода

[
    {"game_field": []}
]

Будет лог

-Строка: 1
-Позиция: 5
-Подстрока параметра: 1
-Подпозиция параметра: 19 # Учти пробелы перед параметром!

================

А куда делась строка

-Команда: (например, include или UnknownCommand)

из логов ошибок?? Верни!

================

Убери все Exceptions из CommandNode._log_error()! Мне нужно, чтобы программа не прерывалась и отрабатывала коррректно всегда, выводя при этом максимальный лог. Сейчас иногда param_subposition = None при подстановке в ValidationError. Это приводит к тому, что

-Подпозиция параметра: <не найдено>

Перепиши _log_error() так, чтобы всегда была гарантия числового значения поля param_subposition!

================

И перепиши все комментарии по-русски!

================

Нет, сейчас строки и позиции пишутся некорректно, не соотвтетсвтуют значения. Я уже реализовывал логику их вычисления в CommandPath.get_line_and_pos() и CommandPath.find_command_element(). Используй эту же логику (если хочешь, перенеси в другой класс, например, ValidationError, если это позволяет парадигма ООП).

Логика для 

-Строки
-Позиции

(НЕ параметра)

должна сохраниться ТА ЖЕ.

Для параметра, полагаю, придется скопировать и немного изменить find_command_element().

Двигайся маленькими шагами. Для начала скопируй старую логику. 

ПИШИ ВСЕ КОММЕНТЫ ПО-РУССКИ!!